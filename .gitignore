# dependencies
node_modules/
.pnp
.pnp.js

# next
.next/
out/
build/
next-env.d.ts

# env — never commit real keys
.env
.env.local
.env*.local
.env.development.local
.env.test.local
.env.production.local

# testing
coverage/
test-results/
playwright-report/
blob-report/
.playwright/
.auth/

# local supabase-compatible stack
.local-stack/
*.pid

# misc
.DS_Store
*.pem
*.log
npm-debug.log*
.vercel
.turbo
tsconfig.tsbuildinfo

# release gate — screenshots are large and regenerated every run. They are
# evidence for a human to look at when a stage reports something, not an input
# to any comparison [D-79], so the repository keeps the text evidence and the
# CI artifact keeps the pictures.
quality/runs/*/screenshots/
